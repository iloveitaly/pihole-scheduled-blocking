
# without this, `pi.hole` magic DNS may not be available
# `ifconfig eth0 | rg 'inet ([0-9.]+) ' -r '$1' -o`
FTLCONF_REPLY_ADDR4='192.168.7.35'

# replace with your own timezone
TZ='America/Denver'

# replace with a strong password
WEBPASSWORD='password'

# Optional: Customize cron schedules for blocking/allowing domains
# If not defined, the default values shown below will be used
# BLOCK_TIME='0 20 * * *'  # Block domains at 8pm (default)
# ALLOW_TIME='0 8 * * *'   # Allow domains at 8am (default)
# ENABLE_BLOCKING_TIME='0 0 * * *'  # Re-enable Pi-hole blocking at midnight (default)
